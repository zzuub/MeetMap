# MeetMap 프론트엔드 작업 이력

> 짝 문서
> - `docs/dev-plan.md` — **계획.** 무엇을 어떤 순서로 만들 것인가
> - `docs/spec/00-index.md` — **사양.** 화면별 기능 상세
> - `docs/decisions.md` — **근거.** 되돌리기 어려운 판단과 그 이유 (원래 이 문서 4장)
> - `docs/phase1-notes.md` — **착수 노트.** Phase 1 요점·막힌 것
> - `src/README.md` — **규칙.** FSD 레이어 구조
> - **이 문서** — **현황.** 지금 어디까지 왔고 최근에 무엇을 했는가

---

## 0. 갱신 규칙

**기능을 추가하는 PR 은 이 문서도 같은 PR 안에서 갱신한다.** 나중에 몰아서 쓰면 "왜 그렇게 했는지"가 이미 사라진 뒤다.

| 무엇을 | 어디에 |
| --- | --- |
| 완료한 작업 ID (P0-1 등) | 2장 진행 현황 표의 상태를 갱신 |
| PR 단위 작업 내용 | 3장 맨 위에 새 항목 추가 (최신순) |
| **되돌리기 어려운 판단** | [decisions.md](decisions.md) 맨 아래에 추가 — 대안·근거·번복 조건까지 |
| 미룬 것 / 막힌 것 | 5장에 추가하거나, 해소되면 제거 |

`decisions.md` 가 이 기록의 핵심이다. 코드는 *무엇을* 했는지 보여주지만 *왜* 는 보여주지 않는다. 3개월 뒤 "이거 왜 이렇게 돼 있지"에 답하는 게 그 문서다.

**3장은 이력 8건을 넘기지 않는다.** 9건째를 쓸 때 가장 오래된 것부터 [progress-archive.md](progress-archive.md) 3장 맨 위로 내린다 — 내용은 그대로 옮기고 지우지 않는다. 이 문서는 **세션마다 읽히므로** 길이가 곧 비용이다. "길어지면 내린다"로 뒀더니 4일 만에 12건이 쌓여 2.75배가 됐다.

같은 이유로 **3장 항목은 왜가 아니라 무엇을 적는다.** 근거·대안·번복 조건은 `decisions.md` 가 원본이고 여기서는 번호로 가리킨다.

기록할 가치가 없는 것: 오타 수정, 포맷팅, 자명한 리팩터링.

---

## 1. 현재 상태 요약

기준일 **2026-09-11**

> ⚠️ **2026-09-01 도메인 재정의.** 제품이 "로테이션 소개팅 통합 탐색 플랫폼"으로 좁혀지면서 데이터 모델·필터 축이 바뀌었다. **2026-09-02 P1-0 으로 `src/` 반영을 마쳤다** — 타입·상수·목데이터·목 API 가 새 모델이다 → 4.6, 4.13~4.15. 남은 것은 **화면**과 **목업**이다(5장).
>
> ⚠️ **2026-09-02 평점 귀속 재정의.** 후기·평점이 **회차가 아니라 주최사에 쌓인다**(배민 가게 리뷰 구조). 회차 평점은 정의상 성립하지 않는 값이었다 → 4.19. `provider` 를 객체로 승격하고 주최사 소개 페이지(기능정의서 7.4)를 신설한다. **문서·데이터 모델 반영을 마쳤다**(P1-0b) — 남은 것은 주최사 페이지 화면(P5-4)이다.

| 항목 | 값 |
| --- | --- |
| 진행 단계 | **Phase 2 완료 (2026-09-11).** P2-1~P2-9 — DoD(신규 가입자 완주 · 찜이 서버에 남는다)에 찜 목록·검색까지 붙었다. 다음은 **Phase 3**(지도·비교 — [phase3-notes.md](phase3-notes.md)). 이월분은 뷰 토글·**거리순 정렬 축**(P3-1)·비교 슬롯(P3-4)·주최사 링크(P5-4) → 4.29·4.36·4.56 |
| 소스 파일 | 246개 (테스트 제외, `src/**/*.{ts,tsx}`) / 약 14,400줄 |
| 테스트 | 620개 (52파일), `npm test` 전부 통과 — UTC·LA·Kiritimati 포함 |
| 스택 | Next.js 16.3.3 · React 19.2.4 · TypeScript 5 · Tailwind CSS v4 · Vitest 4 |
| 백엔드 | Spring Boot 스켈레톤. **실 API 없음** → 목 데이터로 개발 중 |
| CI | `lint` → `test` → `build` (frontend) / `gradlew build -x test` (backend) |
| 보안 | `npm audit` 0건 |

---

## 2. Phase 진행 현황

작업 ID 는 `docs/dev-plan.md` 기준.

### Phase 0 — 공통 기반 ✅ 완료

| ID | 작업 | 상태 |
| --- | --- | --- |
| P0-1 | 디자인 토큰 (보태니컬 화이트, Tailwind v4 `@theme`) | ✅ |
| P0-2 | 라우트 그룹 셸 `(main)` / `(stack)` / `(onboarding)` | ✅ |
| P0-3 | `shared/ui` 13종 + `shared/lib` 8종 | ✅ |
| P0-4 | 공통 피드백 패턴 (Sheet · Modal · Toast) | ✅ |
| P0-5 | `fetchClient` + `ApiError` 정규화 | ✅ |
| P0-6 | 목 데이터 레이어 (`EventApi` 인터페이스 + mock/http) | ✅ |
| P0-7 | 인증·역할 모델 + `proxy.ts` 라우트 가드 | ✅ |
| P0-8 | 도메인 타입 (기능정의서 12장) | ✅ |
| — | Vitest 도입 + 순수 함수 단위 테스트 | ✅ (#5) |

### Phase 1 — USER 코어 퍼널 ✅ 완료

| ID | 작업 | 상태 | 비고 |
| --- | --- | --- | --- |
| P1-0 | **데이터 모델 코드 반영** (타입 · 상수 · 목데이터 · 목 API) | ✅ | 기능정의서 12장 확정본. 4.6 의 결정을 `src/` 에 옮긴 것 |
| P1-1 | `EventCard` 5개 variant | ✅ | `entities/event/ui/`. 카드 전체 링크는 stretched link, 찜·신청 버튼은 `action` 슬롯 (4.16) |
| P1-2 | 배지 · 정원 표기 · 성별 기준 가격 | ✅ | 성비 게이지 삭제 (4.6). 조각 타이포는 부모가 정한다 (4.17) |
| **P1-0b** | **`provider` 객체 승격 + 회차 평점 삭제** | ✅ | `entities/provider` 신설. 목 데이터를 **주최사 4곳 × 회차 2건**으로 재구성 (4.19·4.21) |
| **P1-2b** | 카드·조각의 `provider` 참조 수정 | ✅ | `event.provider.name`. 카드에 평점을 넣지 않는다 |
| **P1-2c** | **코드 품질 정리** (동작 변경 없음) | ✅ | `EventCard` 202줄 함수 → 레이아웃 5파일(4.22), 주석 28%, 테스트 id 결합 제거 |
| P1-3 | 홈 `/` | ✅ | `widgets/home-feed` + `HomeHeader`. 세 섹션 모두 **모집 중만** — 그래서 `feature` 의 상태 배지를 뺐다 (4.23) |
| P1-4 | 탐색 리스트 `/explore` | ✅ | `features/event-filter`(URL 계약) + `widgets/explore-board`. 읽기 방향은 끝났고 **쓰기(컨트롤)는 P1-5·P1-6** 이 붙는다. 하단 탭 `지도`→`탐색` 개편 포함 |
| P1-5 | 필터 시트 | ✅ | `features/event-filter` 에 시트·칩·상단 컨트롤이 붙었다. 3층 + 실시간 반영, 드래프트 상태 없음 |
| **P1-5c** | **적용 필터 칩 줄** | ✅ | 개별 해제·초기화. 시트의 `초기화` 와 대상이 다르다 (4.26) |
| P1-5b | 지역(구) 선택 시트 | ✅ | 시/도 → 구 2단 + 상단 지역 버튼. 구별 `N곳` 은 **현재 필터 반영** (4.28) |
| P1-6 | 정렬 / ~~뷰 토글~~ | ✅ 정렬만 | `SortSelect` 5종. 게스트는 가격 정렬을 못 보고, 그 판정은 **파싱 한 곳**이다 (4.30). **뷰 토글은 P3-1 로 이월** — 지도가 없어 절반이 자리표시자로 간다 (4.29) |
| P1-7 | 소개팅 상세 `/events/[id]` | ✅ 상세만 | `widgets/event-detail`. 카드가 처음으로 목적지를 갖는다. **하단 CTA 3종·주최사 페이지 링크는 이월** — 목적지가 P1-8·P2-7·P3-4·P5-4 라 셋 다 눌러도 아무 일이 없다 (4.32). `참석자 정보` 는 집계값을 다시 쓰지 않고 원본 링크만 맡는다 (4.33) |
| P1-8 | 외부 신청 이동 모달 | ✅ | `features/event-apply` — 버튼과 모달이 한 덩어리라 모달 없는 신청 경로를 만들 수 없다. 하단 고정 CTA 를 세우면서 **찜·비교 자리는 슬롯으로 비웠다** (4.36). 마감 회차는 **막지 않고 모달이 알린다** — 7.3 의 TBD 를 닫았다 (4.37) |
| P1-9 | 빈 상태 / 에러 / 로딩 | ✅ | **11.2 의 에러 카드는 `error.tsx` 에 못 산다** — 클라이언트 경계라 `code`·`occurredAt`·`retryable` 이 프로덕션에서 사라진다. 페이지가 잡아 그리고 `error.tsx` 는 최후 방어선이다 (4.40). 배치는 **에러는 셸마다, 로딩·없는 페이지는 화면마다** (4.41). 0건 액션은 상수가 아니라 **실제로 풀리는 축**을 고른다 (4.42) |

### Phase 2 — USER 온보딩 · 개인화 ✅ 완료

| ID | 작업 | 상태 | 비고 |
| --- | --- | --- | --- |
| P2-1 | 소셜 로그인 `/onboarding` | ✅ | 백엔드 OAuth 가 없어 **`accountApi` 포트**를 세웠다 — `eventApi` 와 같은 형태이고 돌려주는 값만 `REDIRECT`/`SESSION` 유니온이다 (4.43). `?redirect=` 를 처음으로 읽고 `safeRedirect` 로 막는다 (4.45) |
| P2-2 | 약관 동의 `/onboarding/terms` | ✅ | `TERMS_ITEMS` 4항목. 비활성 라벨이 사유를 말한다 (2.5). 여기가 **온보딩의 경계**다 — `isNewUser` 가 여기서 내려간다 (4.43). `자세히 보기` 는 목적지 TBD 라 안 그렸다 |
| P2-3 | 인트로 `/onboarding/intro` | ✅ | 두 갈래 다 링크다. `나중에 할래요` 의 상태를 따로 저장하지 않는다 — **프로필 없음이 곧 그 상태**다 |
| P2-4 | 프로필 설정 `/onboarding/profile` | ✅ | 닉네임 12자·출생연도·성별·지역 3개 한도(초과 시 토스트). **관심 카테고리 없음.** `viewer` 주입이 여기서 풀린다 (4.44) |
| P2-5 | 온보딩 완료 `/onboarding/done` | ✅ | 3.5 동적 문구. **서버에서 프로필을 다시 읽어** 새로고침을 견딘다 (4.48). `홈으로 이동` 은 이제 위치 권한 화면을 거친다 |
| — | **`viewer` 주입** | ✅ | 홈·탐색의 `viewer={null}` 을 풀었다. 인증 축 게이트가 `isGuest: session === null` → **`hasViewer: viewer !== null`** (4.44) |
| — | `(onboarding)` 그룹 경계 | ✅ | `error.tsx`(출구 포함) + 퍼널 공통 `loading.tsx`. `not-found.tsx` 는 안 만들었다 (4.47) |
| P2-6 | 위치 권한 `/onboarding/location` | ✅ | 3상태 + `denied` 사유 3종. **`주변` 의 축은 좌표가 아니라 지역**이고(4.53) 좌표는 서버 액션 안에서만 산다(4.54). 역지오코딩 포트 `entities/geo` 신설. **거리순 축은 P3-1 로 미뤘다** — 되돌릴 문구를 테스트가 잠근다 (4.56) |
| P2-7 | 찜 | ✅ | 카드 5종·상세 CTA 의 빈 슬롯을 채웠다. **TanStack Query 를 안 쓴다**(4.58) · 원본은 `likedIds` 하나이고 상태는 **화면마다 하나**(4.59) · 실패는 토스트(4.60). ⚠️ 리뷰가 시킨 계측이 **토스트 소멸 타이머 결함**을 찾아 함께 고쳤다 (4.61) |
| P2-8 | 찜 목록 `/likes` | ✅ | 본문은 `eventApi.getByIds`(4.62) · 카드는 새 변형 `liked` — `compact` 에 조건을 붙이지 않았다(4.63) · 목록은 **낙관 상태로 거른다** — `revalidatePath` 로는 즉시 제거가 안 된다(4.64) · 정렬 `다가오는 순 → 지난 순`, `편집` 은 안 그렸다(4.65) |
| P2-9 | 검색 `/search` | ✅ | 검색어는 **URL(`?q=`)이 원본이고 조회는 페이지(서버)** — 실패 표면이 늘지 않는다, `replace` 만 쓴다(4.66) · 카드는 새 변형 `search` — `list` 에는 지역이 없다(4.67) · 인기 검색어 포트 `entities/search` 신설, 추천 칩도 거기서(4.68) · 최근 검색어 6개 · 스냅샷 셋(4.69) · 정렬은 `orderUpcomingFirst` 로 내려 찜 목록과 같이 쓴다, `필터` 는 안 그렸다(4.70) |

Phase 3~7 은 `docs/dev-plan.md` 참조.

---

## 3. 작업 이력

### 2026-09-11 · [#29](https://github.com/zzuub/MeetMap/issues/29) · P2-9 리뷰 반영
**`결과 있는 검색어만` 은 백엔드 신뢰에 전부 기댄다 — 막지 않고 관측 지점을 뒀다**

리뷰는 여덟 관점에서 결함을 찾지 못했다 — URL 을 마운트 때만 읽는 전제를 깨는 경로 · 컨트롤러의 경쟁 조건 · 실패 표면 · 저장소 · 카드 유니온 · 동적 라우트 · 문서를 코드와 대조해 확인했다. **짚은 것은 하나**다: 인기 검색어 계약 5번(`결과가 있는 검색어만`)은 스냅샷과 클릭 사이를 프론트가 방어하지 않아, 어겨지면 4.42 가 금지한 "누르면 0건"이 조용히 재발한다.

**막지 않고 관측한다** → 4.68 리뷰 반영 절. 막으려면 `idle` 을 열 때마다 인기 검색어 여섯을 검색해야 한다(추천 칩에서 버린 안과 같은 비용). 0건 화면은 추천 칩 때문에 **이미 인기 검색어를 들고 있어서**, 지금 검색어가 거기 있으면(`isTrending`) 계약 위반으로 `console.warn` 을 남긴다 — 조회가 늘지 않는다. 4.28 이 신호라 적어 놓고 관측 지점이 없어 PR #25 리뷰에 걸린 모양을 이번엔 리뷰의 제안보다 한 걸음 더 막았다. 화면은 이미 성립한다 — 그 0건 화면의 추천 칩에서 같은 검색어가 빠져 되돌아가는 고리가 없다.

리뷰의 제안대로 **16장 기술 결정에 미확인 항목**으로 올렸고, 계약서(`ENDPOINTS.search.trending` 5번)와 phase2-notes 막힌 것 표에도 같은 줄을 적었다. 3장이 9건이 되어 가장 오래된 #25 첫 판을 [progress-archive.md](progress-archive.md) 로 내렸다.

### 2026-09-11 · [#29](https://github.com/zzuub/MeetMap/issues/29) · P2-9 검색 — Phase 2 끝
**예약(`결과 카드는 list`)을 코드와 맞춰 보니 틀렸다 — 지역이 없으면 결과가 왜 걸렸는지 안 보인다**

`/search` 를 만들었다(루트 `not-found` 로 가던 자리). 착수 전에 다섯을 정하고 **검색 상태 × 진입 경로 23줄**을 표로 먼저 셌다 → 4.66.

- **검색어의 원본 · 조회 자리** → 4.66. URL(`?q=`)이 원본이고 조회는 **페이지(서버)** 다 — 클라이언트 조회였으면 4.40 에 셋째 자리가 생겼다. 커밋은 전부 `replace` 라 검색 화면은 히스토리에 한 칸이고, 입력창은 마운트 때만 URL 을 읽는다. 입력창은 `search/layout.tsx` 에 있다(페이지면 `loading.tsx` 경계 안이다)
- **결과 카드** → 4.67. 새 변형 `search`(84px · 상태·시간대 배지 · 하이라이트 · `주최사 · 일시 · 지역` · 가격 · 찜). 예약(`list` 에 하트)은 **틀렸다** — `list` 에는 지역이 없어 `건대`·`역삼` 처럼 지역으로만 걸린 결과가 이유를 못 댄다(4.51 표 열째 줄). 검색어는 유니온 prop 이라 `search` 에만 넘길 수 있다(`tsc` 가 막는다)
- **인기 검색어 · 추천 검색어** → 4.68. 포트 `entities/search` 신설, 계약(순위=배열 위치 · `previousRank` · 오프셋 포함 `baseAt` · **결과가 있는 검색어만**)은 `ENDPOINTS.search.trending`. 결과 없음의 추천 칩도 인기 검색어 앞 4개(지금 검색어 제외)다 — 고정 칩의 `심야` 는 검색 대상 필드가 아니었다. 목 키워드가 결과를 내는지는 `_consistency/trendingKeywords.test` 가 센다
- **최근 검색어 · 정렬** → 4.69·4.70. Enter·결과 클릭·칩만 남긴다 · **6개** · 스냅샷 셋(`unknown`/`ready`/`unavailable`). 정렬은 찜 목록의 것을 `entities/event` 의 `orderUpcomingFirst` 로 내려 함께 쓴다. `필터` 는 안 그렸다(동작 TBD)

**처음 적은 근거 하나가 틀렸다** — 보관 개수를 8로 두고 "375×667 에서 인기 검색어 6위까지 보인다(브라우저 확인)"라고 적었는데, 두 단어 검색어로 재니 4줄이 되어 6위가 19px 접혔다. **6개로 줄이고 잰 표를 4.69 에 남겼다.**

⚠️ **빌드가 `/search` 를 프리렌더하려다 멈췄다** — 페이지가 `searchParams` 를 읽는데도 레이아웃의 `useSearchParams` 가 먼저 `missing-suspense-with-csr-bailout` 로 걸렸다. 레이아웃이 `await connection()` 으로 동적을 못 박는다. `assert-dynamic-routes` 의 바늘에 `searchApi` 를 더했다(인기 검색어 목도 실행 시각에 기댄다).

**jsdom 없이 DOM 고리 다섯을 잠갔다** → 4.61 P2-9 표. debounce 는 `createSearchController` 를 떼어 가짜 타이머로, Enter·✕ 는 `SearchField` 를 함수로 불러, 결과 클릭은 `isResultLinkClick` 으로, 저장소는 가짜 `Storage` 로 — 목록에 셋째가 생기지 않았다. ESLint(`react-hooks/refs`)가 렌더 중 ref 전달을 막아 입력창이 콜백 ref 로 컨트롤러에 **스스로 붙게** 했다.

**phase2-notes 의 예약 둘을 코드로 확인했다** — `/search` 가 루트 `not-found` 로 간다는 것은 참이었고, `결과 카드(list)에 하트` 는 위에 적은 대로 틀렸다. P2-8 PR(#43)은 `main` 에 머지돼 있었고 4.64 표 12·14·17번은 `5b75a81` 로 들어가 있었다.

**Phase 2 를 닫았다** — 1·2장 · phase2-notes(요점 · 막힌 것) · 새 [phase3-notes.md](phase3-notes.md)(넘어온 것은 처방이 아니라 확인할 자리) · session-handoff(196줄) · 16장 · 4장 색인(4.43~4.70). 3장이 0장 규칙(8건)을 한참 넘어 있어 #24~#20 의 15건을 [progress-archive.md](progress-archive.md) 로 내렸다.

검증: `eslint`·`tsc`·`vitest 620건`(52파일, 신규 119)·`next build`·`assert-dynamic-routes`(7개 전부 `ƒ` — `/search` 포함)·`TZ` 2종(PowerShell, offset 420 / −840 확인). **변이 36종이 전부 잡힌다**(테스트 35 + `tsc` 1 — `keyword` 를 `list` 에 넘기기·`search` 에 안 넘기기). 브라우저에서 표 23줄 중 19줄을 확인했다 — 홈 → 검색(입력창 포커스) · 입력 300ms 전후 · 결과(하이라이트·지역) · 0건(추천 칩 → 결과) · 공백·낱자(커밋 안 함) · ✕·다시 검색하기(비우고 포커스) · 결과 → 상세 → 헤더 뒤로가기(같은 결과, 포커스는 안 줌) · 최근 검색어 저장·개별·전체 삭제 · 저장소 차단 흉내(섹션이 빠진다) · 검색 실패·인기 검색어 실패(목에 심었다가 되돌림) · `?q=`·`?q=%20%20` 직접 진입의 서버 HTML. **못 본 넷** — 8번 대소문자(목 데이터에 로마자가 없다 → 합성 입력 테스트), 20번 게스트 하트(목 세션이 HttpOnly 라 이 브라우저에서 로그아웃할 길이 없다 → `viewer.test` 가 목적지를 리터럴로 잠근다), 21번 찜·프로필 조회 실패(기존 `loadViewerContext` 경로), 23번(그리지 않았다). ⚠️ 도구의 Enter 키는 `keydown` 만 보내 폼이 제출되지 않는다 — 16번은 `requestSubmit()` 으로 핸들러를 확인했다.

### 2026-09-11 · [#28](https://github.com/zzuub/MeetMap/issues/28) · P2-8 찜 목록
**`revalidatePath` 로는 `즉시 제거` 가 안 된다 — 목록이 낙관 상태를 읽는다**

`/likes` 자리표시자를 실제 화면으로 바꿨다. 착수 전에 넷을 정했다.

- **본문 조회** → 4.62. `eventApi.getByIds(ids)` + `GET /events/batch?ids=`. `?expand=` 는 `entities/user` 가 `EventSummary` 를 못 부르고, `eventApi` 에 "내 찜"을 물으면 목이 쿠키를 못 읽어서 받지 않았다. 찜 id 는 페이지가 서버에서 읽어 인자로 넘긴다. `EventListQuery` 는 그대로다
- **카드** → 4.63. 새 변형 `liked`(66px · 상태 배지 · `날짜 · 가격 · 거리`). `compact` 에 조건을 달면 홈에서 늘 `신청 가능` 만 찍힌다. `거리` 는 상세처럼 모르면 뺀다
- **해제 피드백** → 4.61 에 이어 적었다. 결과 토스트 그대로 — 카드가 사라지는 것이 즉각 피드백이다. 되돌리기는 TBD 로 남겼고 **이 화면에는 되돌릴 길이 없다**는 것을 16장에 적었다
- **정렬 · `편집`** → 4.65. KST 오늘 이후는 가까운 순, 지난 회차는 최근 것부터 뒤로. `편집` 은 그리지 않았다

**목록이 `LikeProvider` 의 낙관 상태로 거른다** → 4.64. phase2-notes 의 예약(`사라지는 것은 revalidatePath 가 처리한다`)이 틀렸다 — 서버가 다시 그린 뒤다. 계측: 카드 16ms · 토스트 1016ms. 찜 상태 × 진입 경로 16줄을 표로 먼저 셌다. **찜 목록은 `loadViewerContext` 를 쓰지 않는다** — 찜 조회 실패를 `[]` 로 삼켜 틀린 빈 상태가 된다.

**즉시 제거는 브라우저 전용 셋째 항목이 아니다** → 4.61. 리듀서를 `toggleLikedId` 로 떼어 잠갔고, DOM 이 필요한 고리는 클릭 하나(1번과 같은 핸들러)다. 1번 줄을 넓혔다.

⚠️ **`/design-system` 이 P2-7 부터 500 이었다** — `LikeProvider` 가 변형 섹션만 두르고 경계값 섹션의 찜 버튼은 밖에 있었다. 동적 라우트라 빌드·CI 가 조용했다. 공급자를 올리고 렌더 테스트로 잠갔다 (4.51 표).

변이 18종이 전부 잡힌다(로컬 달력을 읽는 `isPastDayKst` 는 UTC·LA·Kiritimati 셋 다 · 쇼케이스 공급자를 `main` 의 모양으로 되돌린 것 포함). ⚠️ **Windows 의 Git Bash 는 `TZ` 를 넘기지 않는다** — 처음 돌린 두 TZ 가 실제로는 KST 였다. PowerShell 로 다시 돌렸다.

### 2026-09-10 · [#27](https://github.com/zzuub/MeetMap/issues/27) · P2-7 찜
**Phase 1 내내 미뤄 온 판단을 여기서 닫았다 — TanStack Query 를 안 쓴다**

찜 토글을 붙였다. 카드 5종의 `action` 슬롯과 상세 하단 CTA 의 `like` 슬롯이 **P1 부터 비어 있던 자리**인데(4.16·4.36) 이번에 채웠다.

**TanStack Query 를 도입하지 않는다** → 4.58. dev-plan blocking #6 의 기본안(`도입 권장`)을 받지 않았다. 근거로 적힌 둘 중 **`무한 스크롤` 은 P1-6 이 라이브러리 없이 이미 만들었고**(`더 보기` 는 `useState` 세 줄이다), 남은 `찜 낙관적 업데이트` 는 `useOptimistic` + `revalidatePath` 로 끝난다. 도입하면 **RSC 페이로드와 쿼리 캐시가 같은 값을 둘로 든다** — 4.53 이 정리한 그 모양이다. 잃는 것(화면 밖 캐시·자동 재검증)과 번복 조건(P3-1 지도의 뷰포트 조회)을 함께 적었다.

**찜 상태의 원본은 `likedIds` 하나다** → 4.59. 12장이 회차마다 둔 `isLiked` 를 **화면이 읽지 않는다.** ⚠️ 목이 그 값을 사용자별로 채울 수 없기 때문이다 — `eventApi` 는 `더 보기` 때문에 **클라이언트에서도 도는 모듈**이라 `next/headers` 를 쓸 수 없다. 목의 한계를 화면이 몰라도 되도록 두 모드가 같은 경로(`userApi.getLikedIds()`)를 탄다. 브라우저에서 확인했다: 목 데이터에 `isLiked: true` 가 박힌 `evt-005` 가 찜 안 된 상태로 나온다.

**상태는 화면마다 하나다**(`LikeProvider`) → 4.59. 버튼마다 `useOptimistic` 을 두는 쪽이 짧지만, **홈은 같은 회차가 섹션 셋에 겹쳐 뜬다** — 실제로 `evt-002` 가 둘, `evt-003` 이 셋이다. 각자 상태를 들면 하나를 눌렀을 때 같은 화면에 서로 다른 답이 그려진다. 겹친 하트 둘이 함께 켜지는 것을 눌러서 확인했다.

**되돌리기 코드가 없다** → 4.59. `useOptimistic` 의 기준값이 서버에서 온 prop 이라 실패하면 낙관값만 걷히고 서버 값이 드러난다 — 구조가 보장한다. 그리고 API 는 **토글이 아니라 `PUT`/`DELETE`** 다: 낙관적 업데이트가 화면을 먼저 그리므로 토글이면 재시도·중복 클릭이 상태를 뒤집는다.

**찜 실패는 토스트다** → 4.60. 4.46 의 `FormErrorNotice` 를 쓰지 않는다. 기준을 한 문장으로 적었다 — **사용자가 잃을 것이 있으면 화면에 남기고, 없으면 스쳐 보낸다.** 폼은 잃을 것이 있고 찜은 하트가 제자리로 돌아오는 것으로 이미 보인다.

⚠️ **15장이 찜 버튼을 이름으로 지목해 뒀다** (`현재 26~30px → 확대 또는 히트영역 확장 필요`). 처음에 36px 버튼을 직접 만들었다가 `IconButton` 의 대역 주석에서 걸렸다 — 그 컴포넌트가 이미 44px 하한의 주인이다. **다시 만들지 않고 그것을 쓰되 `size` 를 하나 더했다**(`cta` 48px, 7.2). `cn` 이 Tailwind 충돌을 해결하지 않으므로 크기를 밖에서 덮어쓰지 않고 안에서 고른다.

**디자인 시스템의 대역을 실물로 바꿨다.** 그 화면의 목적이 "공통 컴포넌트가 실제로 어떻게 보이는가"라 대역은 답을 못 준다. 토글은 화면 안에서만 살고 서버에 남지 않는다.

**목업이 리포로 편입되면서 린트에 걸렸다** — `docs/mockups/support.js` 는 캔버스 런타임이라 우리가 고칠 파일이 아니다. 규칙을 맞추는 대신 `eslint.config.mjs` 의 무시 목록에 더했다.

**리뷰가 시킨 계측이 다른 결함을 찾았다** → 4.61. "낙관적 업데이트의 깜빡임은 정적으로 확정할 수 없으니 느린 액션으로 재현해 보라"는 제안대로 `revalidatePath` 앞에 1.5초를 넣고 40ms 간격으로 관찰했다. **깜빡임은 없었고**(하트가 한 번만 뒤집힌다) 실패 경로도 되돌리기 코드 없이 제자리로 왔다 — 대신 **토스트가 6초를 지켜봐도 안 떴다.**

원인은 찜이 아니라 `ToastProvider` 였다. `showToast` 가 **호출 시점**부터 1.8초를 셌는데, `startTransition` 안의 갱신은 transition 이 끝날 때까지 커밋되지 않는다 — 액션이 1.8초보다 길면 커밋 전에 타이머가 만료돼 문구가 지워진다. **타이머를 이펙트로 옮겨 커밋 뒤부터 세게 했다.** 고친 뒤: 하트 55ms, 토스트 772ms 등장 → 2592ms 소멸(1.8초 유지).

덤으로 알게 된 것 — 이 토스트는 낙관이 아니라 **결과**다. transition 이 끝날 때 커밋되므로 `저장했어요` → `못 했어요` 짝이 생기지 않고 실패 토스트만 뜬다. 4.60 이 우연히 더 정확해졌다.

**리뷰 지적 하나를 더 받았다**: 히트 영역 테스트가 `size-(11|12|14)` 로 **허용 값을 열거**하고 있었다. `size-14` 는 죽은 분기이고, 새 variant 가 생길 때마다 사람이 목록을 갱신해야 한다. **규칙 자체로 바꿨다** — `size-N` 은 N×4px 이므로 `N×4 ≥ 44`. 양방향으로 확인했다(36px·40px 은 검출, 56px 은 통과).

⚠️ **이 항목의 문서가 소스보다 먼저 커밋됐다.** 목업 편입 PR(#39)에 문서 변경만 딸려 들어가고 커밋되지 않은 소스가 유실돼, 한동안 `main` 이 **P2-7 을 ✅ 로 적어 두고 코드는 없는** 상태였다. 소스는 이 문서를 사양 삼아 다시 지었고 결과는 같다(변이로 확인). **커밋되지 않은 작업은 이렇게 사라진다** — 세션 규칙 5가지에 `main 에 MR 해도 되는지` 를 넣은 이유가 이것이다.

검증: `eslint`·`tsc`·`vitest 468건`(신규 16)·`next build`·`assert-dynamic-routes`·`TZ` 2종. 새 테스트는 **변이 8종**으로 확인했다 — 히트 영역 하한 36px 로 축소 / `IconButton` 우회 / `aria-pressed` 제거 / 라벨을 상태와 무관하게 / 저장·해제 토스트를 같은 문구로 / 다른 회차의 찜이 이 버튼을 켜기 / 게스트에게 로그인 목적지를 안 주기 / `redirect` 를 인코딩하지 않기. ⚠️ **처음엔 셋이 살아남았고** 그중 하나는 **테스트가 `likeLabel` 끼리 비교**하고 있어서였다(4.52 에서 잡은 함정과 같은 모양). 리터럴로 바꿨다. `preventDefault` 누락은 DOM 이벤트가 필요해 테스트로 못 잡고 **브라우저에서 확인**했다(하트를 눌러도 상세로 안 간다).

브라우저에서 왕복을 확인했다: 홈에서 찜 → 겹친 카드 둘이 함께 켜짐 → 탐색에서도 켜져 있음 → `더 보기` 로 받은 항목도 정확 → 상세 48px 원형에서 해제 → 홈으로 돌아오면 꺼져 있음.

### 2026-09-10 · [#26](https://github.com/zzuub/MeetMap/issues/26) · P2-6 위치 권한
**상수 하나를 바꾸라고 적어 뒀는데, 그 상수는 뜻이 둘이었다**

`/onboarding/location` 3상태(`asking`/`granted`/`denied`)를 `(funnel)` 안에 붙였다. 퍼널이 여섯 화면이 됐고 완주 경로가 이 화면을 지난다.

**`AFTER_ONBOARDING` 을 바꾸지 않았다** → 4.52. `phase2-notes.md` 가 "상수 하나를 바꾸면 된다"고 예약해 뒀지만 읽는 자리가 둘이고 뜻이 다르다 — `done` 의 `홈으로 이동`(퍼널의 다음 걸음)과 `signInLanding` 의 폴백(**기존 회원**의 로그인 목적지)이다. 바꿨다면 `?redirect=` 없이 로그인한 기존 회원이 매번 위치 권한 화면을 봤다. ⚠️ **`landing.test.ts` 가 상수끼리 비교하고 있어 그걸 못 잡았다** — 3.1 이 못 박은 목적지를 리터럴로 잠갔다. `ONBOARDING_STEPS.location` 을 더하고 `done` 의 링크만 돌렸다.

**`주변` 의 축을 좌표가 아니라 지역으로 정했다** → 4.53. `EventListQuery` 에 좌표가 없고, 2.4 가 필터 상태의 원본으로 삼은 URL 에 좌표를 실을 수 없다(공유·북마크·로그에 남는다). 대신 **역지오코딩이 좌표를 `area` 한 개로 되돌린다** — 요약 카드가 세는 집합과 `주변 소개팅 보기`(`/explore?area=…`)가 여는 집합이 같은 축이 된다. 4.2 의 `/explore?view=map` 으로 보내지 않았다: 지도는 P3-1 자리표시자이고 **자리표시자는 `주변` 을 반영하지 않아** 퍼널의 마지막 걸음이 조건 없는 빈 화면으로 떨어진다.

**활동 지역(4.3)은 선호 지역(3.4)과 한 값이다** → 4.53. 마스터도 한도도 같아서 둘로 두면 어긋난다 — `HomeFeed.baseAreaLabel` 의 계약이 이미 "권한 거부 시 **선호 지역명**으로 대체된다"고 적고 있었다. 3.4 에서 고른 지역을 미리 선택해 보여주고 같은 필드에 저장한다. ⚠️ `saveProfile` 로는 저장할 수 없어(프로필을 건너뛴 사용자도 여기 도달한다) **`savePreferredAreas` 좁은 쓰기**를 세웠다 — 지역만 남고 `getMyProfile` 은 계속 `null` 이라 `viewer` 가 근거 없이 살아나지 않는다 (4.44).

**역지오코딩 포트를 세웠다 (`entities/geo`)** → 4.54. `ENDPOINTS` 에 지오코딩 항목이 아예 없었다. 슬라이스를 따로 판 이유는 소유자가 없어서다 — 좌표→지명은 event 의 성질도 user 의 성질도 아니고, 한쪽에 넣으면 다른 쪽이 동일 레이어 참조로 막힌다. **좌표는 서버 액션 안에서만 살고 URL·쿠키·클라이언트 상태 어디에도 남지 않는다** — 4.1 의 고정 문구가 화면이 지킬 수 있는 약속이 되도록 자리를 표로 세어 막았고, 백엔드가 지켜야 할 것도 `ENDPOINTS.geo.reverse` 에 적었다.

**거리순 축을 만들지 않았다** → 4.56. 4.3 의 `거리순 정렬 비활성` 은 **비활성할 대상이 없다**(`SORT_OPTIONS` 5종에 거리순이 없다). 6.2 자신이 TBD 로 적었고, 정렬의 입력인 좌표를 URL 에 실을 수 없어 축을 만들면 **URL 로 표현할 수 없는 탐색 축**이 생겨 6.1 계약이 깨진다. 그래서 4장 문구 중 셋(4.1 혜택 ①② · 4.2 `반경 3km` · 4.3 제약 안내)을 지금 참인 문구로 바꿨고, **되돌릴 문구는 `MAP_AXIS_PHRASES` 가 들고 테스트가 거리 축의 유무로 양방향 단언한다** — 누가 `SORT_OPTIONS` 에 `distance` 를 더하는 순간 빨간불이 켜진다.

**3상태는 클라이언트 상태 하나에 두고 세 화면이 서로 오간다** → 4.55. 4.2 의 `권한 요청 화면 다시 보기`(실서비스 노출 TBD)를 없애지도 그대로 두지도 않고 **`지역 직접 선택할게요` 로 이름과 목적지를 바꿨다** — 없애면 권한을 허용한 사용자가 지역을 고를 길이 사라져 상태가 일방통행이 되고, 그대로 두면 프로토타입 컨트롤이 남는다. 그 결과 브라우저 권한을 되돌리지 않고도 개발 중에 세 화면에 전부 도달한다. **개발 전용 스위치를 만들지 않았다.**

**`denied` 로 떨어지는 길이 셋이다** — 권한 거부 / 측위 실패·타임아웃 / 미지원. 배너 문구가 갈리고, 모르는 오류 코드를 `PERMISSION` 으로 읽지 않는다(켤 것이 없는 설정으로 보내게 된다). 진입 경로 × 상태 11줄을 **표로 먼저 적고** 시작했다 (4.55).

**실패 표면 둘을 다르게 잡는다** — 진입의 선호 지역 조회는 `loadOrError` → `ApiErrorScreen` 이고 **온보딩 셸이라 `홈으로 이동` 을 남긴다**(4.40·4.47, `done` 과 같은 형태). 좌표 해석·건수 조회 실패는 화면을 갈아끼우지 않고 **누른 버튼 위 한 줄**(`FormErrorNotice`, 4.46)이다 — 그 버튼 자체가 재시도다.

검증: `eslint`·`tsc`·`vitest 451건`(신규 36)·`next build`·`assert-dynamic-routes`(5개 전부 `ƒ`)·`TZ` 2종. 새 테스트는 **변이 13종**으로 전부 실패를 확인했다 — `isTodayKst` 의 KST 보정 제거 / `nearestArea` 를 배열 순서로 / 개인정보 고지 감추기 / `denied` 배너를 하나로 / 모든 실패를 권한 거부로 / 없는 기능(`거리순 정렬`) 약속하기 / 비활성 라벨에서 사유 빼기 / 못 센 건수를 0 으로 / 오늘 필터 빼기 / `AFTER_ONBOARDING` 을 위치 권한으로 / `location` 단계의 가드 전부 제거 / 0건에도 `N건을 찾았습니다` / 못 센 오늘 건수 그리기. ⚠️ 처음엔 11종만 잡히고 **`granted` 의 0건·못 셈 분기 2종이 살아남아** 테스트를 더했다.

### 2026-09-10 · [#25](https://github.com/zzuub/MeetMap/issues/25) · P2-1~P2-5 3차 리뷰 반영
**세는 일을 사람에서 테스트로 옮겼다. 그리고 그 테스트가 처음엔 그 버그를 못 잡았다**

3차 리뷰는 결함을 찾지 못했다 — 가드 네 자리 전수 확인, `intro` 의 `없음` 판단, `(onboarding)` 밖에 같은 형태가 없다는 것(세션 가드가 붙은 레이아웃은 리포에 `(funnel)` 하나뿐이고, 역할 게이트는 `proxy.ts` 라 RSC 요청 자체를 가로챈다), `fetchClient` 옵션 선반영이 실이라는 것까지 다 확인만 하고 넘겼다. 대신 **내가 물어본 것에 답을 줬다**: 4.51 을 표가 아니라 **파일 트리 스캔 테스트**로 막으라는 것. 그것만 했다.

**`_guard.test.ts` 를 만들었다** → 4.51. `(funnel)/` 의 단계 폴더를 훑어 `requireFunnelSession(` 호출을 요구하고, `_guard.ts` 의 **`NO_SESSION_STEPS` 에 사유가 적힌 단계만** 통과시킨다. `assert-dynamic-routes.mjs`(4.31)가 "눈으로 하던 확인을 그대로 자동화"한 것과 같은 형태이고, 사유를 `Record` 로 요구하는 것은 `REQUIRED_PHRASES`(4.39)와 같은 구조다.

**판정 방향이 전부다** — 기본이 `부르라` 이고 안 부르려면 사유를 적어야 한다. 반대로 두면 P2-6 을 폴더에 넣고 아무것도 안 해도 통과해 세 판 동안 반복한 실수가 그대로 되돌아온다. 빈 `location/page.tsx` 를 실제로 만들어 테스트가 깨지는 것을 확인했다.

**그리고 처음 만든 테스트는 그 버그를 못 잡았다.** 이름만(`requireFunnelSession`) 찾아서 **호출을 지우고 `import` 줄만 남겨도 통과**했다 — 그게 마침 2차 리뷰가 찾은 `done` 누락 그 자체다. 변이 테스트가 걸렀고("통과하는 테스트를 믿지 않는다"), 지금은 **괄호까지 찾고 주석을 걷어낸 뒤** 센다. 변이 8종(단계 넷의 호출 제거 · 주석 처리 · 면제 해제 · 면제인데 부르기 · 빈 신규 단계)이 전부 실패하는 것을 확인했다.

**여기까지만 했다.** 같은 처방을 실패 표면(#24)·진입 경로(#25)에도 원리상 적용할 수 있다는 리뷰 지적에 동의하지만 범위를 넘고, 무엇보다 **그 스캔이 무엇을 셀지부터 정해야** 한다 — 자동화가 틀린 축을 세면 사람이 세는 것보다 나쁘다.

`fetchClient` 에 헤더 반환 옵션을 미리 두지 않는다는 판단도 리뷰 근거를 그대로 받았다 — 두 선택지 중 하나에만 유효하고, 호출자 없는 옵션은 테스트도 못 붙으며, "계약을 지켜도 연결될 자리가 없다"는 지금의 명확한 실패(4.50)가 "옵션은 있는데 아무도 안 부른다"는 더 헷갈리는 상태로 바뀔 뿐이다.

검증: `eslint`·`tsc`·`vitest 415건`(신규 8)·`next build`·`assert-dynamic-routes`·`TZ` 2종.


### 2026-09-10 · [#25](https://github.com/zzuub/MeetMap/issues/25) · P2-1~P2-5 2차 리뷰 반영
**세 번 연속으로 같은 실수를 했다 — 이번엔 쓰기를 세고 읽기를 안 셌다**

리뷰가 둘을 짚었고 둘 다 맞다. 하나는 가드 구멍, 하나는 **계약을 지켜도 작동하지 않는 상태**다.

**① `done/page.tsx` 가 가드를 안 불렀다** → 4.49 정정. 1차 리뷰 반영에서 `requireFunnelSession` 을 떼면서 "부르는 자리가 셋"이라고 적었는데 **넷이어야 했다.** `done` 은 퍼널 단계(3.5)이고 `<Link>`(인트로의 `나중에 할래요` · 프로필의 `건너뛰기`)로만 도달하는데다 **프로필을 실제로 조회한다.** 4.49 가 측정한 "레이아웃 재실행 보장 없음" 케이스에 정확히 걸린다.

빠뜨린 이유가 정확히 기록할 값이 있다 — **기준을 "쓰기를 하는 자리"로 잡았기 때문**이다. `done` 은 폼이 아니라 조회라 그 기준에 안 걸렸고, 내가 적은 한계 서술("폼이 그려지는 것까지는 못 막는다")도 이 경우를 안 덮었다. 기준을 **"세션에 딸린 일을 하는가"** 로 바꾸고 **단계 넷을 표에 전부 적었다** — `intro` 가 `없음` 으로 표에 남아 있는 것이 요점이다. 빠진 칸이 아니라 **비어 있는 칸으로 보여야** 다음 사람이 다시 안 빠뜨린다.

**② 약관 POST 의 세션 갱신은 계약을 지켜도 작동하지 않는다** → 4.50. 1차 반영에서 "백엔드가 `Set-Cookie` 로 토큰을 내려야 한다"를 `endpoints.ts` 에 계약으로 적었는데, 리뷰가 `fetchClient` 를 따라가 **받을 자리가 없다**는 것을 찾았다. `agreeTermsAction` → `saveTerms` 는 Server Action 안에서 나가는 **서버→서버 fetch** 이고 `fetchClient` 는 `response.headers` 를 어디서도 안 읽는다(확인했다). 백엔드가 계약을 완벽히 지켜도 그 헤더는 Next 서버에서 끝난다.

**"프론트가 위반을 감지 못 한다"가 아니라 "계약을 지켜도 작동하지 않는다"** 라 등급이 다르다. OAuth 콜백은 브라우저가 직접 리다이렉트를 타서 멀쩡하고, 이 리포에서 차이가 나는 자리는 **약관 POST 하나뿐**이다.

**지금 구현하지 않는다** — 실 백엔드가 없어 눌러볼 수 없고(3.1 인증 실패 토스트를 안 만든 기준과 같다), 무엇보다 어느 모양으로 받을지가 백엔드 응답에 달렸다. 대신 두 선택지(① `Set-Cookie` 릴레이 ② 응답 본문으로 토큰)를 계약서 자리(`endpoints.ts`)에 적고 `phase2-notes.md` 막힌 것에서 **이번 Phase 에서 가장 무거운 미결**로 올렸다. 리뷰가 제안한 "제출 후 재조회로 확인"은 **릴레이 뒤에야 의미가 있다** — 지금 붙이면 항상 갱신 전 세션을 봐서 오탐이 된다.

**세 번 반복된 형태라 번호를 붙였다** → 4.51. #24 는 화면만 세고 컨트롤을 안 셌고(표면 2→3), #25 는 화면을 세고 시간을 안 셌고(진입 경로 7→8), 이번엔 쓰기를 세고 읽기를 안 셌다(호출 3→4). 셋 다 잊어버린 것이 아니라 **세는 축을 하나 빠뜨린 것**이다. 그래서 표를 적을 때 **축을 먼저 적고 빈 칸도 남긴다** — `emptyRelaxation.test.ts` 가 `Record` 로 축 14개를 강제한 것(4.42)과 같은 발상이고, 타입으로 못 강제하는 자리에서는 표가 그 일을 한다.

리뷰가 확인만 하고 넘긴 `getServerSession.ts` 의 `USE_MOCK` 분할은 그대로 뒀다 — 판정 범위를 함수 하나로 좁힌 것으로 모순이 해소됐다는 판단에 동의한다.

검증: `eslint`·`tsc`·`vitest 407건`·`next build`·`assert-dynamic-routes`·`TZ` 2종. 게스트가 `/onboarding/done` 을 열면 로그인으로 떨어지는 것을 실제로 확인했다.


### 2026-09-10 · [#25](https://github.com/zzuub/MeetMap/issues/25) · P2-1~P2-5 코드리뷰 반영
**표를 먼저 세서 화면은 다 셌는데, 시간축을 안 셌다**

리뷰가 셋을 짚었고 셋 다 손댔다. 구현 결함은 하나도 아니었다 — 전부 **검증되지 않은 전제**였다.

**① 퍼널 가드가 소프트 내비게이션에서 다시 도는지 확인이 안 됐다.** 주장을 좁히기 전에 **측정했다** — 레이아웃에 로그를 심고 개발 서버 stdout 을 읽었다 → 4.49. 하드 로드와 Server Action 의 `redirect()` 에서는 항상 돌지만 **`<Link>` 이동에서는 보장되지 않는다.** 두 번 측정해 답이 달랐다(한 번은 아예 안 찍혔고, 한 번은 프리페치로 보이는 호출이 있었다). "안 돈다"도 과장이라 `보장되지 않는다` 로 적었는데, 결론은 오히려 분명해진다 — **타이밍에 기대는 가드는 가드가 아니다.**

이게 4.45 의 진입 경로 표에 없던 여덟 번째 줄이다: **퍼널 안에서 세션이 죽는 경우.** 진입할 때는 세션이 있었으니 표의 어느 줄에도 안 걸린다. 화면은 다 셌는데 시간축을 안 센 것이 지난 판의 "표면을 둘로 셌는데 셋이었다"와 같은 종류의 누락이다.

판정을 `(funnel)/_guard.ts` 의 `requireFunnelSession` 하나로 떼고 **쓰기를 하는 액션 둘이 한 번 더 부른다**(Server Action 은 매번 서버에서 돈다). 화면 넷에 각각 적는 안과 `checkAccess` 로 올리는 안은 받지 않았다 — 후자는 `PUBLIC_PREFIXES` 가 `/onboarding` 전체를 열어 두어 "아래는 막고 자신은 열어라"를 프리픽스 한 줄로 못 쓴다. **남는 것을 정확히 적었다**: 폼이 그려지는 것은 못 막고, 세션 없는 저장과 실 모드 401 의 잘못된 안내를 막는다. 액션 가드가 실제로 도는 것은 로그로 확인했다(제출 → 액션 자신 + `redirect()` 후 레이아웃, 둘 다 찍힌다).

**② `getServerSession.ts` 가 자기 파일 안에서 모순이었다.** 주석은 "목/실 분기는 `readSession` 안에 있다, 여기서 다시 판정하지 않는다"인데 바로 아래 `getAccessToken` 이 `USE_MOCK` 을 또 본다. 코드를 지우지 않고 **주석을 정확히 고쳤다** — 둘은 성격이 다르다. 하나는 "세션을 어디서 읽나", 다른 하나는 **"토큰을 밖으로 내보낼까"** 다. 실 모드에서 쓰던 `meetmap_at` 이 남은 채 목 모드로 내려오면 목 구현이 값을 무시한다는 사실에만 기대게 되는데, 목 구현은 여럿이고 늘어난다.

**③ 빈 `finishSignUp` 이 백엔드 계약에 기대는데 그 계약이 어디에도 없었다.** `POST /users/me/terms` 가 약관을 저장하면서 **갱신된 토큰을 `Set-Cookie` 로 심어 `isNewUser` 를 내려야** 한다. 안 지키면 실 모드에서 기존 회원이 로그인할 때마다 약관 화면으로 되돌아오는데 **프론트에는 확인할 수단이 없다** — 목에 재현 경로가 없어 테스트 407건 중 아무것도 못 잡는다. 근거가 주석 한 줄뿐이었으므로 **프론트-백 계약서 자리인 `shared/api/endpoints.ts` 의 `user.terms`** 에 옮겨 적고, 포트 주석과 `phase2-notes.md` 막힌 것 표에서 그리로 가리킨다.

리뷰가 확인만 하고 넘긴 것 둘은 그대로 뒀다. `safeRedirect` 는 추가 우회가 없고 소비 지점도 하나가 맞다(로그인 화면이 hidden input 에 넣기 전에 한 번 통과시키는 것은 중복 검증이지 별도 경로가 아니다). `hasViewer` 게이트도 재판정 자리가 없다. 리뷰가 덤으로 찾은 조합 — 역할 스위치를 켠 채 로그인하면 실제로는 USER 인데 `찜`·`마이` 가 막힌다 — 은 "스위치가 로그인을 이긴다"의 파생이라 `phase2-notes.md` 개발 팁에 한 줄 더했다.

검증: `eslint`·`tsc`·`vitest 407건`·`next build`·`assert-dynamic-routes`·`TZ` 2종. 가드 변경은 순수 함수가 아니라 테스트가 아니라 **로그로 확인**했다.


> **#25 첫 판(P2-1~P2-5 온보딩 퍼널) 이전 이력은 [progress-archive.md](progress-archive.md) 에 있다.**
> 이 장은 *최근* 만 남긴다 — 길어지면 완료된 Phase 부터 아래로 내린다(0장).

---

## 4. 주요 기술 결정

전문은 [decisions.md](decisions.md) 에 있다. 필요한 항목만 펴 본다.

| # | 결정 |
| --- | --- |
| 4.1 | FSD 레이어 경계를 ESLint 로 강제한다 |
| 4.2 | `shared/config` 에 도메인 마스터 데이터를 둔다 (`shared` 원칙의 예외) |
| 4.6 | 도메인 재정의 — 목업에 있으나 실재하지 않는 값을 걷어낸다 |
| 4.3 | 목/실 API 를 인터페이스 하나로 분기한다 |
| 4.4 | 계정과 역할을 분리한다 (`Account` 1:N `Membership`) |
| 4.5 | ADMIN 은 소셜 로그인을 쓰지 않는다 |
| 4.7 | 서버 렌더에서는 fetch 에 `signal` 을 붙이지 않는다 |
| 4.8 | `Sheet` / `Modal` 은 `document.body` 로 포털한다 |
| 4.9 | `Intl.DateTimeFormat` 인스턴스를 공유한다 |
| 4.10 | Vitest 를 쓰되 jsdom 은 붙이지 않는다 |
| 4.11 | 의존성은 정확한 버전으로 고정한다 (캐럿 미사용) |
| 4.12 | `proxy.ts` 는 UX 가드이지 보안 경계가 아니다 |
| 4.13 | `stationName` 을 `EventSummary` 에 추가한다 (기능정의서 12장 초과) |
| 4.14 | 남녀 정원은 동수를 전제하되 필드는 둘로 유지한다 |
| 4.15 | 직업군은 닫힌 마스터가 아니라 자유 입력 태그다 |
| 4.16 | 카드는 stretched link 로 만들고 액션은 슬롯으로 비운다 |
| 4.17 | 카드 조각은 타이포를 부모에 위임한다 |
| 4.18 | `thumbnailUrl` 을 nullable 로 바꾼다 (기능정의서 12장 갱신) |
| 4.19 | 평점은 회차가 아니라 주최사에 쌓인다 |
| 4.20 | 평점 정렬은 하드 컷이 아니라 베이지안 보정을 쓴다 |
| 4.21 | 평점 산식을 `entities/provider` 가 아니라 `shared/lib` 에 둔다 |
| 4.22 | 카드 변형은 분기가 아니라 레이아웃 컴포넌트로 나눈다 |
| 4.23 | 홈은 마감된 소개팅을 배지로 표시하지 않고 아예 거른다 |
| 4.24 | 하단 탭의 두 번째는 `지도` 가 아니라 `탐색` 이다 |
| 4.25 | URL 은 기본값을 싣지 않는다 — `eligibleOnly` 만 예외다 |
| 4.26 | `초기화` 가 둘이고 지우는 대상이 다르다 |
| 4.27 | 게스트 게이트는 파싱 한 곳뿐이고, 화면은 키의 유무로 읽는다 |
| 4.28 | 축별 건수는 현재 필터를 반영하고, 못 세면 건수를 포기한다 |
| 4.29 | 뷰 토글은 P1-6 이 아니라 지도가 생기는 P3-1 에서 붙인다 |
| 4.30 | 게스트의 가격 정렬은 화면이 아니라 파싱에서 막는다 |
| 4.31 | 목 데이터의 날짜는 이번 주 월요일 기준 상대값이다 |
| 4.32 | `/providers/[id]` 링크와 하단 고정 CTA 를 P1-7 에서 붙이지 않는다 |
| 4.33 | 상세의 `참석자 정보` 는 집계값을 다시 쓰지 않고 원본 링크만 맡는다 |
| 4.34 | `AppHeader` 에 타이틀이 없으면 그 화면이 `h1` 을 직접 갖는다 |
| 4.35 | 서버 값을 화면에서 한 번 더 검사할지 정하는 사다리 |
| 4.36 | 하단 CTA 는 `신청하기` 하나로 세우고 찜·비교 자리는 슬롯으로 비운다 |
| 4.37 | 마감 회차의 `신청하기` 는 막지 않고 모달이 알린다 |
| 4.38 | 7.3 우회 경로를 문서가 아니라 린트가 막는다 |
| 4.39 | 법적 고지가 **그려지는지**는 jsdom 없이도 잠글 수 있다 |
| 4.40 | 11.2 의 에러 카드는 `error.tsx` 가 아니라 페이지가 그린다 |
| 4.41 | 에러는 셸마다, 로딩·없는 페이지는 화면마다 |
| 4.42 | 0건 빈 상태의 액션은 상수가 아니다 |
| 4.43 | 로그인 버튼은 `accountApi` 포트를 세운다 — 목 역할 쿠키에 묶지 않는다 |
| 4.44 | `viewer` 주입을 P2-4 와 같은 판에 넣는다 — 게이트는 세션이 아니라 프로필이다 |
| 4.45 | 온보딩의 진입 경로를 표로 세고, `?redirect=` 는 소비 시점 한 곳에서 검증한다 |
| 4.46 | 네 번째 실패 표면 — 제출 실패는 폼 안에서 알린다 |
| 4.47 | `(onboarding)` 그룹 경계는 화면 수가 아니라 출구 때문에 붙인다 |
| 4.48 | 온보딩 입력값은 단계마다 제출한다 — 2.4 의 `일괄 제출` 에서 벗어난다 |
| 4.49 | `(funnel)` 레이아웃 가드는 모든 이동에서 다시 돌지 않는다 |
| 4.50 | 서버 액션의 `Set-Cookie` 는 브라우저까지 못 간다 |
| 4.51 | 세 번 연속으로 같은 실수를 했다 — 규칙을 만들고 적용될 자리를 안 셌다 |
| 4.52 | `AFTER_ONBOARDING` 은 상수 하나가 아니었다 — 뜻이 둘이었다 |
| 4.53 | `주변` 의 축은 좌표가 아니라 지역이다 |
| 4.54 | 좌표는 서버 액션 안에서만 살고 어디에도 남지 않는다 |
| 4.55 | 3상태는 클라이언트 상태 하나에 두고, 세 화면이 서로 오간다 |
| 4.56 | 없는 기능을 약속하지 않는다 — 되돌릴 문구를 테스트가 들고 있다 |
| 4.57 | 자동화도 단위를 잘못 잡으면 조용히 통과한다 |
| 4.58 | TanStack Query 를 도입하지 않는다 |
| 4.59 | 찜 상태의 원본은 `likedIds` 하나이고, 화면마다 하나만 둔다 |
| 4.60 | 찜 실패는 토스트다 — 네 번째 표면의 경계 |
| 4.61 | 토스트의 소멸 타이머는 커밋 뒤부터 센다 (+ 브라우저로만 확인하는 항목의 목록) |
| 4.62 | 찜 목록의 본문은 `eventApi.getByIds` 로 받는다 |
| 4.63 | 찜 목록 카드는 새 변형 `liked` 다 |
| 4.64 | 찜 목록은 서버 prop 이 아니라 낙관 상태를 그린다 |
| 4.65 | 찜 목록 정렬은 `다가오는 순 → 지난 순`, `편집` 은 만들지 않는다 |
| 4.66 | 검색어의 원본은 URL(`?q=`)이고 조회는 서버가 한다 — 검색 상태 × 진입 경로 표 |
| 4.67 | 검색 결과 카드는 `list` 가 아니라 새 변형 `search` 다 |
| 4.68 | 인기 검색어는 `entities/search` 포트다 — 추천 검색어도 여기서 온다 |
| 4.69 | 최근 검색어는 `검색했다`고 말할 수 있을 때만 남긴다 — 6개, 첫 렌더는 `모른다` |
| 4.70 | 검색 결과는 `다가오는 순 → 지난 순` 이다 · `필터` 미구현 · 4.34 셋째 소비자 |

---

## 5. 미해결 항목

Phase 착수를 막는 것부터. 전체 목록은 `docs/dev-plan.md` 4장 + 기능정의서 16장.

| 항목 | 막는 것 | 상태 |
| --- | --- | --- |
| **목록 응답에 패싯(faceted) 카운트가 없다** | 서버 (Phase 5+) | **P1-5b 에서 방식은 정했다** → 4.28. `api/exploreFacets.ts` 가 조건에 맞는 건을 긁어와 클라이언트에서 버킷팅한다 — 조회 **2회**(축마다 1회)이고, 상한을 넘기면 그 축은 `null` 로 돌려 **건수를 포기**하며 그때 `console.warn` 을 남긴다.<br>⚠️ **비용은 미래형이 아니라 현재형이다** (PR #25 리뷰). 지금도 화면에 **6건**을 그리려고 스캔이 **최대 600건**(300×2)을 실어 나른다 — 호출 수는 줄었지만 호출당 페이로드는 늘었다. 데이터가 수백 건만 돼도 레이턴시가 먼저 체감되고, `null` 이 뜨는 것은 그다음이다.<br>남은 것은 **서버 `facets`** 다. 목록 응답에 실려 오면 조회가 1회로 줄고 전건 스캔이 사라진다. `exploreFacets` 내부만 갈아끼우면 되고 호출부·화면 규칙은 안 바뀐다. **갈아탈 신호는 위 `console.warn`** 이다 |
| ~~목 데이터의 날짜가 늙어 테스트 1건이 깨진다~~ | — | **해소.** 2026-09-07, 날짜를 **이번 주 월요일 기준 상대값**으로 바꿨다 → 4.31. 기준이 `isThisWeek` 와 같은 `weekRangeKst` 라 `이번 주 5건 · 그 이후 3건` 배치가 구조로 보장된다. 요일·타임존을 바꿔 가며 확인했고 손으로 갱신할 일이 없어졌다 |
| **탐색 조건 변경 중 `총 N개 소개팅` 이 이전 값으로 서 있다** | 아직 아무것도 | **P1-9 가 절반만 닫았다.** `loading.tsx` 는 붙었지만 **조건 변경에는 뜨지 않는다** — 칩·시트·칩 줄이 전부 `startTransition` 안에서 이동해 이미 마운트된 경계가 유지되기 때문이다(실제로 눌러 확인했다 → 4.41). 그래서 폴백이 뜨는 자리는 최초 진입·새로고침·탭 이동뿐이고, 조건 변경 중에는 P1-5 가 넣은 **누른 자리의 표시 셋**만 일한다. 결과 수까지 덮으려면 그 숫자에 표시를 붙이거나 이동을 트랜지션 밖으로 빼야 하는데, 후자는 표시 셋을 전부 잃는다 |
| **`/explore` 콘솔 400 오류** | 아직 아무것도 | 페이지 로드마다 400 이 여러 건 찍힌다. **P1-4 베이스라인에서도 동일**하므로(2026-09-05 stash 로 확인) P1-5 가 만든 것이 아니다. 화면 동작에는 영향이 없어 보이나 원인 미확인 — 네트워크 로그가 요청을 안 잡아 개발 오버레이 쪽일 가능성이 있다 |
| ~~도메인 재정의 코드 반영~~ | — | **해소.** 2026-09-02 P1-0 에서 타입·상수·목데이터·목 API 교체 완료(3장). 화면은 P1-1 부터의 통상 작업이라 블로커가 아니다 |
| **홈 `신규 입점 주최사` 섹션 신설 여부** | P1-3 홈 | 평점순 정렬이 기존 주최사에 유리하므로 신규에게 균형추가 필요하다는 제안. **현재 보류.** ① 섹션 C(`새로 등록된 소개팅`)와 카드가 거의 겹치고 ② 주최사 7개뿐인 지금은 "신규 입점"이 사실상 전체이며 ③ 다른 3개 섹션은 소개팅 카드인데 이것만 주최사 카드라 새 변형이 필요하다(14.1 의 5종에 없음). **재검토 조건**: Phase 6 셀프서비스 등록이 열려 주최사가 스스로 들어올 때, 또는 섹션 C 가 특정 주최사에 쏠릴 때 |
| **`sort=rating` 의 주최사 뭉침** | P1-6 | 평점 상위 주최사가 회차를 여러 건 올리면 목록 상단을 독점한다. 2차 정렬(개최일)만 두었고 주최사당 연속 노출 상한은 없다 → 4.20 |
| **평점 산식 상수 `C`·`m`** | P1-6 (서버) / Phase 5 | `C=10` `m=4.3` 으로 시작. 실제 후기 분포가 쌓이면 재산정. 프론트는 영향 없다 |
| ~~**`ratio`·`compact` 카드의 모집 상태 표시**~~ | — | **2026-09-04 해소.** 배지를 늘리는 대신 **홈이 마감을 아예 거른다** → 4.23. 남았던 찜 목록 쪽은 **2026-09-11(#28)** 에 `compact` 에 넣지 않고 새 변형 `liked` 로 풀었다 → 4.63 |
| **`/policy/terms` 화면이 없다** | **실서비스 출시** | 3.1 `약관 링크` · 3.2 `자세히 보기` 가 둘 다 이 화면을 가리키고 화면은 **TBD** 다. 목적지 없는 링크를 만들지 않는 기준(4.29)에 따라 두 자리를 비워 뒀는데, **약관 전문을 볼 수 없는 동의 UI 는 그대로 내보낼 수 없다.** 개발 단계에서는 아무것도 막지 않지만 출시 전에는 막는다. `Checkbox` 의 `trailing` 슬롯이 3.2 쪽 자리다 |
| ~~**화면의 `viewer` 주입**~~ | — | **해소.** 2026-09-10 P2-4 와 같은 판에서 넣었다 → 4.44. 홈·탐색이 `userApi.getMyProfile()` 결과를 그대로 `viewer` 로 넘긴다(`UserProfile` 이 `EventCardViewer` 를 구조적으로 만족한다). 인증 축 게이트는 세션이 아니라 프로필을 본다(`hasViewer`) — 로그인만 하고 프로필을 건너뛴 사용자가 생겼기 때문이다. **상세(7.1)는 그대로다** (4.32) |
| **세션은 살아 있는데 프로필 조회가 401** | 아직 아무것도 | `loadViewerContext` 가 실패를 삼키고 `viewer=null` 로 떨어뜨리므로(4.44) 헤더는 로그인 상태인데 가격이 게스트 기준(남·여 병기)이 된다. 화면으로는 성립하지만 사용자에게는 설명이 안 된다. **목에는 재현 경로가 없어** 지금 닫지 않는다 — 실 API 가 붙을 때 `UNAUTHORIZED` 를 세션 만료로 읽어 로그인으로 보내는 것이 그 자리다 |
| **3.1 인증 실패 토스트** | 아무것도 | 실 모드에서 인증 실패가 드러나는 자리는 **OAuth 콜백**이고 백엔드 몫이다. 목에는 실패 경로가 없어 지금 만들면 눌러볼 수 없는 코드가 된다 → 4.46. 붙일 자리는 `/onboarding?error=…` 를 읽는 로그인 화면이다 |
| **인앱 브라우저에서 `/`·`/explore` 의 Suspense 경계가 안 풀린다** | 아직 아무것도 | 브라우저 패널로 열면 `loading.tsx` 폴백이 끝까지 남는다. **서버는 완전한 HTML 을 돌려준다** — 같은 페이지에서 `fetch('/')` 로 받으면 피드·가격·배지가 전부 들어 있고 `next build` 도 통과한다. **`git stash` 로 베이스라인(#24 시점)에서도 같은 증상을 확인했으므로 P2-1 이 만든 것이 아니다.** 스트리밍 청크가 패널까지 안 오는 환경 문제로 보이며(패널이 숨겨진 상태에서 재현), 실제 브라우저에서 다시 확인이 필요하다. `/onboarding/*` 은 정상이다 |
| **홈 알림 안읽음 도트** | P1-3 완결 · P4 | `HomeHeader` 에 `hasUnreadNotifications` 자리는 있으나 알림 조회 API 가 없어 항상 `false` 다 (5-3) |
| **목업 갱신** | Phase 1 UI | **2026-09-10 대부분 해소.** 목업이 [`docs/mockups/`](mockups/README.md) 로 들어왔고 잔여석·성비 게이지·단일 가격·`N~N세`는 사라졌다. 남은 셋은 `spec/00-index.md` 하단 표. **코드가 먼저 새 모델로 갔으므로 P1-1 부터는 목업이 아니라 기능정의서를 기준으로 그린다** |
| 직업군 표기 수렴 | Phase 6/7 등록 폼 | **필터 블로커는 해소.** 필터 축이 아니라 표시 전용으로 확정했고 자유 입력 태그로 간다 → 4.15. 남은 것은 등록 폼의 태그 제안 UI·회사명 차단. dev-plan 4장 #11 |
| **주최사 등록 동의 절차·증빙** | Phase 6 | 컨택 → 동의 → 등록. 동의 범위(정보/이미지/참석자 리스트) 기록 위치 미정. dev-plan 4장 #13 |
| **정기 반복 일정 등록** | Phase 6 | "매주 토요일"이 다수. 반복 규칙 없으면 운영자가 매주 수십 건 수기 입력. dev-plan 4장 #12 |
| **참여 인증 정책·API** | Phase 5 전체 | 미정 |
| **Admin · Provider 기능정의서** | Phase 6 / 7 | 목업 10종은 존재하나 화면 상세 기능정의가 문서화되지 않음 |
| 지도 SDK 선정 + 클러스터링 | Phase 3 | 카카오맵 권장, 미확정 |
| ~~서버 상태 라이브러리 (TanStack Query)~~ | — | **2026-09-10 해소(#27).** 도입하지 않는다 → 4.58. 번복 조건은 P3-1 지도의 뷰포트 조회 |
| **스택 헤더 뒤로가기가 히스토리 없는 진입에서 아무것도 안 한다** | 아직 아무것도 | 새 탭으로 연 `/events/[id]`·`/search?q=` 에서 `router.back()` 이 갈 곳이 없다 — 4.29 의 `누르면 아무 일 없는 컨트롤` 이다. 기존 `AppHeader` 의 한계라 P2-9 범위 밖으로 뺐다 (4.66 표 2번 · phase3-notes) |
| 초기 소개팅 데이터 공급 주체 | Phase 6/7 순서 | 운영자 전제로 `USER → ADMIN → PROVIDER` 진행 중 |

### 알려진 기술 부채

- **`cn()` 이 Tailwind 클래스 충돌을 해결하지 않는다** — 문자열 결합만 한다. 컴포넌트 `className` 오버라이드 시 기본값과 겹치지 않는 유틸리티를 넘겨야 한다. 충돌이 실제로 문제되면 `tailwind-merge` 도입 검토. 카드 조각은 이 제약 때문에 타이포를 아예 갖지 않는다 → 4.17
- **터치 타깃 44×44 미달 3건** (15장 기준) — `Chip` 38px / `SegmentedControl` 36px / `Toggle` 30×52px. 전부 P0-3 `shared/ui` 다. `IconButton`(44) 처럼 **시각 크기는 두고 히트 영역만 넓히는** 방식으로 고쳐야 하며, 세 컴포넌트를 손대면 모든 화면의 컨트롤 높이가 바뀌므로 별도 작업으로 뺀다
- **`_next/image` 400 노이즈** — `public/mock/` 이 없어 목 6건의 썸네일이 요청당 3회(srcset) 실패한다. 대체 표시가 받아내지만 콘솔이 시끄러워 진짜 에러가 묻힌다 → 4.18
- ~~**목 주최사 수치가 두 파일에 중복**~~ — **2026-09-03 해소.** `src/app/_consistency/` 의 교차 검증 테스트가 두 entity 의 공개 API 로 값을 대조한다. 중복 자체는 남지만 어긋나면 CI 가 잡는다 → 4.21
- **`PrimaryButton` 이름과 책임 불일치** — `variant="secondary"` 를 받는다. 기능정의서 14장 명칭을 따른 것
- **`.idea/` 가 이미 git 에 추적되고 있다** — `.gitignore` 에 넣어도 효과가 없다. 제거하려면 `git rm --cached` 가 필요하고 backend 까지 영향. 미결정
- **`src/app/test/page.tsx`** — create-next-app 잔재. 라우트 충돌은 없으나 제거 여부 미정

# shared

도메인 지식이 없는 재사용 자원. 이 레이어의 코드는 MeetMap이 소개팅 앱이라는 사실을 몰라도 이해할 수 있어야 한다.

- `ui/` — 버튼, 칩, 뱃지 등 디자인 시스템 컴포넌트
- `lib/` — 날짜/가격 포맷 등 순수 함수
- `api/` — fetch 클라이언트 공통 설정, 인터셉터, 에러 규약
- `config/` — 환경변수, 상수
